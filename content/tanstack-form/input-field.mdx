---
title: Input Field
description: Text input component integrated with TanStack Form for type-safe form management. Supports tooltips and InputGroup integration for addons and buttons.
position: 2
registryName: tsf-input-field
---

## Less verbose code

<CodeHighlight code={`// Traditional TanStack Form
<form.Field
  name="email"
  validators={{
    onChange: ({ value }) => !value.includes('@') ? 'Invalid' : undefined
  }}
>
  {(field) => (
    <div>
      <label>{field.name}</label>
      <input 
        value={field.state.value}
        onChange={(e) => field.handleChange(e.target.value)}
        onBlur={field.handleBlur}
      />
      {field.state.meta.errors && <span>{field.state.meta.errors}</span>}
    </div>
  )}
</form.Field>`} language='tsx' />

With `InputField`, the same result in a single component:

<CodeHighlight code={`<InputField
  form={form}
  name='email'
  label='Email'
  formProps={{
    validators: {
      onChange: ({ value }) => !value.includes('@') ? 'Invalid' : undefined
    }
  }}
/>`} language='tsx' />

## Built-in features

- **Type-safe names**: Autocomplete for field names with `DeepKeys<TFormData>`
- **Tooltip support**: InfoIcon with contextual help
- **InputGroup ready**: Supports shadcn InputGroup for addons/buttons
- **Async validation**: Built-in support for API validation
- **Full type inference**: Value types inferred from form schema

## Common use cases

<CodeHighlight code={`const form = useForm({
  defaultValues: {
    name: '',
    email: '',
    age: 0,
  },
  onSubmit: async ({ value }) => {
    await saveData(value)
  },
})

// Basic text input
<InputField
  form={form}
  name='name'
  label='Name'
  description='Your full name'
/>

// Email with validation
<InputField
  form={form}
  name='email'
  label='Email'
  props={{ type: 'email' }}
  formProps={{
    validators: {
      onChange: ({ value }) =>
        !value.includes('@') ? 'Invalid email' : undefined
    }
  }}
/>

// With tooltip
<InputField
  form={form}
  name='username'
  label='Username'
  tooltip='Username must be unique and 3-20 characters'
  formProps={{
    validators: {
      onChange: ({ value }) =>
        value.length < 3 ? 'Too short' : undefined
    }
  }}
/>

// Number input
<InputField
  form={form}
  name='age'
  label='Age'
  props={{ type: 'number', min: 0, max: 120 }}
/>`} language='tsx' />

<ItemExamples registryName={'tsf-input-field'} /> 

<PropTable props={[
  {
    name: 'form',
    type: 'ReactFormApi<TFormData>',
    description: 'Form instance from useForm hook'
  },
  {
    name: 'name',
    type: 'DeepKeys<TFormData>',
    description: 'Type-safe field name (supports nested paths like "user.email")'
  },
  {
    name: 'label',
    type: 'string?',
    description: 'Field label text'
  },
  {
    name: 'description',
    type: 'string?',
    description: 'Helper text displayed below the input'
  },
  {
    name: 'tooltip',
    type: 'React.ReactNode?',
    description: 'Tooltip content displayed in an info icon button'
  },
  {
    name: 'formProps',
    type: 'Partial<FieldOptions>?',
    description: 'TanStack Form field options (validators, etc.)'
  },
  {
    name: 'fieldProps',
    type: 'React.ComponentProps<typeof Field>?',
    description: 'Props passed to the Field wrapper component'
  },
  {
    name: 'props',
    type: "React.ComponentProps<'input'>?",
    description: 'Native HTML input props (type, placeholder, disabled, etc.)'
  }
]} />
