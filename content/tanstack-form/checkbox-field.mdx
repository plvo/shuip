---
title: Checkbox Field
description: Checkbox component integrated with TanStack Form for type-safe boolean field management. Features label and boxLabel props.
position: 7
registryName: tsf-checkbox-field
---

## Built-in features

- **Dual labels**: `label` (top) and `boxLabel` (next to checkbox, clickable)
- **Type-safe**: Full TypeScript support with TanStack Form
- **Validation**: Sync and async validation support
- **Accessible**: Proper label associations and ARIA attributes

## Common use cases

<CodeHighlight code={`const form = useForm({
  defaultValues: {
    terms: false,
    marketing: false,
    newsletter: false,
  },
  onSubmit: async ({ value }) => {
    await saveData(value)
  },
})

// Required checkbox
<CheckboxField
  form={form}
  name='terms'
  label='Terms and Conditions'
  boxLabel='I accept the terms and conditions'
  description='You must accept the terms to continue'
  formProps={{
    validators: {
      onChange: ({ value }) =>
        !value ? 'You must accept the terms' : undefined
    }
  }}
/>

// Optional checkbox
<CheckboxField
  form={form}
  name='marketing'
  label='Marketing'
  boxLabel='Send me promotional emails'
  description='Optional: Receive updates about new features'
/>

// Without boxLabel (standalone)
<CheckboxField
  form={form}
  name='newsletter'
  label='Enable Newsletter'
  description='Toggle newsletter subscription'
/>

// With conditional validation
<CheckboxField
  form={form}
  name='sameAsShipping'
  label='Billing Address'
  boxLabel='Same as shipping address'
  formProps={{
    validators: {
      onChange: ({ value }) => {
        const shipping = form.getFieldValue('shippingAddress')
        if (value && !shipping) {
          return 'Please enter a shipping address first'
        }
        return undefined
      }
    }
  }}
/>`} language='tsx' />

<ItemExamples registryName={'tsf-checkbox-field'} /> 

<PropTable props={[
  {
    name: 'form',
    type: 'ReactFormApi<TFormData>',
    description: 'Form instance from useForm hook'
  },
  {
    name: 'name',
    type: 'DeepKeys<TFormData>',
    description: 'Type-safe field name (supports nested paths like "preferences.notifications")'
  },
  {
    name: 'label',
    type: 'string',
    description: 'Main field label displayed at the top (required)'
  },
  {
    name: 'boxLabel',
    type: 'string?',
    description: 'Text displayed next to the checkbox (clickable)'
  },
  {
    name: 'description',
    type: 'string?',
    description: 'Helper text displayed below the checkbox'
  },
  {
    name: 'formProps',
    type: 'Partial<FieldOptions>?',
    description: 'TanStack Form field options (validators, etc.)'
  },
  {
    name: 'fieldProps',
    type: 'React.ComponentProps<typeof Field>?',
    description: 'Props passed to the Field wrapper component'
  },
  {
    name: 'props',
    type: 'React.ComponentProps<typeof Checkbox>?',
    description: 'Props passed to the Checkbox component'
  }
]} />
