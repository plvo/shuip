---
title: Select Field (react-hook-form)
description: A select form field designed to be used with react-hook-form and Zod for validation. It combines a label, select dropdown, error handling, and optional description into a single reusable component.
group: form
---

<ItemHeader filename={'rhf-select-field'} />

## Less verbose code in forms

<CodeHighlight code={`<FormField
  control={form.control}
  name="country"
  render={({ field }) => (
    <FormItem>
      <FormLabel>Country</FormLabel>
      <Select onValueChange={field.onChange} defaultValue={field.value}>
        <FormControl>
          <SelectTrigger>
            <SelectValue placeholder="Select a country" />
          </SelectTrigger>
        </FormControl>
        <SelectContent>
          <SelectItem value="us">United States</SelectItem>
          <SelectItem value="ca">Canada</SelectItem>
          <SelectItem value="fr">France</SelectItem>
        </SelectContent>
      </Select>
      <FormDescription>
        Choose your country
      </FormDescription>
      <FormMessage />
    </FormItem>
  )}
/>`} language='tsx' />

With `SelectField`, the same result in a single line:

<CodeHighlight code={`<SelectField 
      register={form.register('country')} 
      label="Country" 
      placeholder="Select a country"
      description="Choose your country"
      options={{ 'United States': 'us', 'Canada': 'ca', 'France': 'fr' }}
/>`} language='tsx' />

## react-hook-form integration

The component is specifically designed for react-hook-form:

<CodeHighlight code={`const form = useForm({
      defaultValues: { category: '' },
      resolver: zodResolver(schema),
});

const options = {
      'Technology': 'tech',
      'Design': 'design', 
      'Business': 'business',
};

// Direct usage with form register
<SelectField 
      register={form.register('category')} 
      label="Category" 
      options={options}
/>`} language='tsx' />

<ItemFooter itemName={'select-field'} props={[
  {
    name: 'register',
    type: 'UseFormRegisterReturn',
    description: 'react-hook-form register function result',
  },
  {
    name: 'options',
    type: 'Record<string, string>',
    description: 'Object where keys are labels and values are form values',
  },
  {
    name: 'label',
    type: 'string?',
    description: 'Label displayed above the field',
  },
  {
    name: 'placeholder',
    type: 'string?',
    description: 'Placeholder text in the select',
  },
  {
    name: 'description',
    type: 'string?',
    description: 'Optional description below the field',
  },
  {
    name: 'defaultValue',
    type: 'TFieldValues[FieldPath<TFieldValues>]?',
    description: 'Default selected value',
  },
  {
    name: '...props',
    type: 'SelectProps',
    description: 'All Radix Select props',
  },
]} />
