---
title: Input Field
description: A form field input designed to be used with react-hook-form and Zod for validation. It combines a label, input, error handling, and optional description into a single reusable component.
group: form
---

<ItemHeader filename={'input-field'} />

## Less verbose code in forms

<CodeHighlight code={`<FormField
  control={form.control}
  name="email"
  render={({ field }) => (
    <FormItem>
      <FormLabel>Email</FormLabel>
      <FormControl>
        <Input placeholder="your@email.com" {...field} />
      </FormControl>
      <FormDescription>
        Your email address
      </FormDescription>
      <FormMessage />
    </FormItem>
  )}
/>`} language='tsx' />

With `InputField`, the same result in a single line:

<CodeHighlight code={`<InputField 
      register={form.register('email')} 
      label="Email" 
      description="Your email address"
      placeholder="your@email.com" 
/>`} language='tsx' />

## Built-in features

- **Automatic type handling**: Native support for `text`, `password`, `number`
- **Visibility toggle**: Automatic eye/eye-off button for password fields
- **Zod validation**: Native integration with react-hook-form and Zod
- **Error display**: Responsive error messages (hidden on mobile at top, shown at bottom)
- **Automatic conversion**: `number` fields automatically convert values

## react-hook-form integration

The component is specifically designed for react-hook-form:

<CodeHighlight code={`const form = useForm({
      defaultValues: { name: '', age: 0 },
      resolver: zodResolver(schema),
});

// Direct usage with form register
<InputField register={form.register('name')} label="Name" />
<InputField register={form.register('age')} label="Age" type="number" />`} language='tsx' />

<ItemFooter itemName={'input-field'} props={[
  {
    name: 'register',
    type: 'UseFormRegisterReturn',
    description: 'react-hook-form register function result',
  },
  {
    name: 'label',
    type: 'string',
    description: 'Label displayed above the field',
  },
  {
    name: 'description',
    type: 'string?',
    description: 'Optional description below the field',
  },
  {
    name: 'type',
    type: 'string?',
    description: 'HTML field type (text, password, email, number...)',
  },
  {
    name: '...props',
    type: 'InputProps',
    description: 'All shadcn/ui Input props',
  },
]} />